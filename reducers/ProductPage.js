const initialState = {
    selectedSizeId: null,
    selectedId: null,
    selectedValue: null,
    selectedTitle: null,
}

const ProductPageReducer = (state = initialState, action) => {
    switch (action.type) {
        default: {
            return state
        }
    }
}

export default ProductPageReducer
